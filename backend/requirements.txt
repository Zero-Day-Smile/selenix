numpy
opencv-contrib-python
scipy
scikit-image
fastapi
uvicorn[standard]
python-multipart
pydantic
imageio
tifffile
pvl
pds4-tools
reportlab
matplotlib
geopandas
shapely
rasterio
rioxarray
xarray
dask[array,distributed]
rio-cogeo
psutil

# Optional deep matcher (LoFTR via kornia). ~700MB CPU wheel — install separately if
# space/bandwidth is a concern; the pipeline falls back to classical SIFT cleanly if absent:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install kornia
